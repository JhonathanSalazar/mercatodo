language: php
php:
  - 7.4
OS: linux
cache:
  directories:
    - node_modules
    - vendor
before_script:
  - cp .env.travis .env
  - composer self-update
  - composer install --no-interaction
  - php artisan storage:link
  - php artisan key:generate

script:
  - vendor/bin/phpunit
