language: php
php:
  - 7.4
cache:
  directories:
    - node_modules
    - vendor
before_script:
  - cp .env.travis .env
  - composer self-update
  - composer install --no-interaction
  - php artisan storage:link

script:
  - php artisan key:generate
  - vendor/bin/phpunit
